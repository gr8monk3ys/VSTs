target-version = "py39"

line-length = 88

[lint]
# Pin the rule set: CI installs the latest ruff, whose default selection has
# grown over time and broken builds. New rules must be adopted deliberately.
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM", "PIE", "PERF", "BLE", "RUF"]
